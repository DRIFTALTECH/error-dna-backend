# list_families

## Purpose
List error-family categories with fix counts. No arguments.

## Input
None.

## Output
List of `{ name, description, color, fix_count }`.  
Use `name` as the `family` filter in `search_errors`.

## See also
- `search_errors(family=...)`
- `hybrid_search`
